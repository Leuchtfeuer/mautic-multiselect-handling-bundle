### Usage

1. Install plugin.
2. Set up custom field(s) with multiselect type, which will contain a list of Segments for synchronization.
3. Add form Field "Mautic multi select fields"
4. Add form action "Change Segment membership"

After form is submitted the Lead will have only segments from the multiselect field from step #2.